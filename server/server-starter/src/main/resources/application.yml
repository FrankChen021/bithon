bithon:
  pipelines:
    traces:
      mapping:
        - type: uri
          args:
            parameters: ["query_id"]
        - type: name
          args:
            names: ["query_id", "clickhouse.query_id"]

management:
  endpoints:
    web:
      exposure:
        include:
          - info
          - health
