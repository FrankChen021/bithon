server:
  port: 9897

bithon:
  tracing:
    globalSanitizer:
      type: uri
      args:
        sensitiveParameters:
          - password
    mapping:
      - type: uri
        args:
          parameters: ["query_id"]
      - type: name
        args:
          names: ["query_id"]