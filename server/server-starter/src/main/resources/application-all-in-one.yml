#
# This all-in-one profile is just an example.
# It combines other profiles to enables webapp, collector and use H2 database as storage.
#
spring:
  profiles:
    include:
      - collector-to-local
      - storage-jdbc
      - webapp